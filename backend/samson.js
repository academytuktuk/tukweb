const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const p = await prisma.player.findFirst({ where: { name: { contains: 'Samson' } } }); if (!p) return console.log('not found'); const inns = await prisma.battingInnings.findMany({ where: { playerId: p.id } }); console.log('Sanju Samson Innings:'); inns.forEach(i => console.log('MatchID: ' + i.matchId + ' Runs: ' + i.runs + ' Balls: ' + i.balls)); } main();
