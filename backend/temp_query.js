const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const player = await prisma.player.findFirst({ where: { name: { contains: 'Nair' } }, include: { BattingInnings: { include: { match: true } } } }); if(!player) return console.log('Player not found'); console.log('Player:', player.name); const inns = player.BattingInnings.filter(i => i.match.season === '2026').map(i => ({ match: i.match.fixture, runs: i.runs, balls: i.balls, date: i.match.date })); console.log(inns); } main().finally(() => prisma.\());
